export type MemberData = {
  members: {
    biography: string
    color: string
    instrument: string
    name: string
    orbitLength: number
    _id: string
  }[]
  paginationInfo: {
    totalMembers: number
  }
}
