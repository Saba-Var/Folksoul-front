export type MemberData = {
  members: {
    orbitLength: number
    biography: string
    instrument: string
    color: string
    name: string
    _id: string
  }[]
  paginationInfo: {
    totalMembers: number
  }
}
